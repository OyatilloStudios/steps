name: X-Step Pro APK Build

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 1. Kodni yuklab olish
      uses: actions/checkout@v4

    - name: 2. Java 21 o'rnatish
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: 3. Xatoliklarni tozalash va muhitni tayyorlash
      run: |
        # Loyiha qaysi papkada ekanligini avtomat topish (x-step-pro ichidagi app papkasini aniqlaydi)
        TARGET_DIR=$(dirname $(find . -name "app" -type d | head -n 1))
        cd $TARGET_DIR
        
        echo "Ishchi papka: $TARGET_DIR"
        
        touch .env .env.example
        echo "sdk.dir=$ANDROID_HOME" > local.properties
        
        if [ -f "app/build.gradle.kts" ]; then
          echo "build.gradle.kts tozalanmoqda..."
          sed -i 's/"ksp"(/add("ksp", /g' app/build.gradle.kts
          sed -i 's/ksp(/add("ksp", /g' app/build.gradle.kts
        fi
        
        # Keystore yaratish
        keytool -genkey -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
        echo "Keystore fayli yaratildi."

    - name: 4. Toza Gradle 9.3.1 dvigatelini o'rnatish
      run: |
        TARGET_DIR=$(dirname $(find . -name "app" -type d | head -n 1))
        cd $TARGET_DIR
        
        wget -q https://services.gradle.org/distributions/gradle-9.3.1-bin.zip
        unzip -q gradle-9.3.1-bin.zip

    - name: 5. Dasturni yig'ish (Build APK)
      run: |
        TARGET_DIR=$(dirname $(find . -name "app" -type d | head -n 1))
        cd $TARGET_DIR
        
        ./gradle-9.3.1/bin/gradle assembleDebug -x lint -x test --no-daemon

    - name: 6. Tayyor faylni olish
      uses: actions/upload-artifact@v4
      with:
        name: X-Step-Pro-APK-Fayl
        path: "**/app/build/outputs/apk/debug/*.apk"
